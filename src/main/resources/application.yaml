
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/time_registry
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update #create-drop